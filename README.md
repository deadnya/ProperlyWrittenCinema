# Cinema Management System

Полнофункциональная система управления кинотеатром, разработанная на Spring Boot с использованием современных технологий и лучших практик разработки.

## 📋 Описание проекта

Это приложение предоставляет comprehensive API для управления кинотеатром, включая:

- **Управление фильмами** - добавление, редактирование и удаление информации о фильмах
- **Управление сеансами** - создание киносеансов с поддержкой периодического расписания
- **Управление залами и местами** - конфигурация залов и категорий мест
- **Система бронирования** - покупка билетов и управление заказами
- **Система платежей** - интеграция с платежными системами
- **Аутентификация и авторизация** - защита данных через JWT токены
- **Рецензии и отзывы** - возможность оставлять отзывы о фильмах
- **Загрузка медиа** - управление постерами и изображениями фильмов

## 🛠 Технологический стек

### Backend
- **Spring Boot 3.5.6** - основной фреймворк
- **Spring Security** - аутентификация и авторизация
- **Spring Data JPA** - работа с базой данных
- **Spring Mail** - отправка электронных писем
- **Flyway** - управление миграциями БД
- **JWT (JJWT)** - работа с JWT токенами
- **MapStruct** - маппирование между DTO и Entity
- **Lombok** - сокращение boilerplate кода
- **Validation** - валидация данных
- **OpenAPI/Swagger** - документация API

### Database
- **PostgreSQL 16** - основная база данных

### Infrastructure
- **Docker & Docker Compose** - контейнеризация приложения
- **Maven** - управление зависимостями и сборка проекта

## 📦 Структура проекта

```
cinema/
├── src/
│   ├── main/
│   │   ├── java/com/absolute/cinema/
│   │   │   ├── controller/           # REST контроллеры
│   │   │   ├── service/              # Бизнес-логика
│   │   │   ├── repository/           # Работа с БД
│   │   │   ├── entity/               # JPA сущности
│   │   │   ├── dto/                  # DTO классы
│   │   │   ├── mapper/               # MapStruct маппер
│   │   │   ├── config/               # Конфигурация приложения
│   │   │   ├── common/               # Общие компоненты
│   │   │   └── CinemaApplication.java
│   │   └── resources/
│   │       ├── application.properties # Локальные конфиги
│   │       ├── application-docker.properties
│   │       ├── db/migration/         # SQL миграции
│   │       ├── static/               # Статические файлы
│   │       └── templates/            # Шаблоны
│   └── test/                         # Юнит-тесты
├── docker-compose.yml                # Docker Compose конфиг
├── Dockerfile                        # Docker образ
├── pom.xml                          # Maven конфигурация
└── README.md
```

## 🚀 Запуск приложения

### Предварительные требования
- Docker и Docker Compose установлены
- Java 21 (если запускаете локально без Docker)
- Maven 3.6+ (если запускаете локально без Docker)

### Способ 1: С использованием Docker (Рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd cinema
   ```

2. **Создайте файл `.env` в корневой директории проекта:**
   ```env
   # Database Configuration
   DB_NAME=cinema
   DB_USER=postgres
   DB_PASSWORD=postgres
   
   # JWT Configuration
   JWT_SECRET=your-secret-key-here-min-32-characters-long
   JWT_ACCESS_EXPIRATION_MS=86400000
   JWT_EXPIRED_TOKENS_CLEANUP_MS=3600000
   
   # Mail Configuration
   SPRING_MAIL_HOST=smtp.gmail.com
   SPRING_MAIL_PORT=587
   SPRING_MAIL_USERNAME=your-email@gmail.com
   SPRING_MAIL_PASSWORD=your-app-password
   SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
   SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
   
   # Logging Level
   LOGGING_LEVEL_COM_ABSOLUTE_CINEMA=INFO
   LOGGING_LEVEL_SECURITY=DEBUG
   LOGGING_LEVEL_SQL=DEBUG
   ```

3. **Запустите приложение:**
   ```bash
   docker compose up
   ```

   Приложение будет доступно по адресу: `http://localhost:8080`
   PostgreSQL будет доступен на `localhost:5432`

4. **Остановка приложения:**
   ```bash
   docker compose down
   ```

### Способ 2: Локальный запуск (без Docker)

1. **Установите PostgreSQL 16** и создайте базу данных:
   ```sql
   CREATE DATABASE cinema;
   ```

2. **Обновите конфигурацию в `application.properties`**

3. **Запустите Maven:**
   ```bash
   mvn clean install
   mvn spring-boot:run
   ```

## 👤 Учетные данные администратора

После первого запуска приложения используйте следующие данные для входа:

- **Email:** `admin@admin.com`
- **Пароль:** `admin`

⚠️ **ВАЖНО:** Измените пароль администратора в production окружении!

## 📚 API Документация

После запуска приложения документация API доступна по адресу:

- **Swagger UI:** `http://localhost:8080/swagger-ui.html`
- **OpenAPI JSON:** `http://localhost:8080/v3/api-docs`

## 🔐 Аутентификация

API использует JWT токены для аутентификации. 

1. Отправьте запрос на login:
   ```bash
   POST /api/auth/login
   Content-Type: application/json
   
   {
       "email": "admin@admin.com",
       "password": "admin"
   }
   ```

2. Используйте полученный `accessToken` в заголовке `Authorization`:
   ```bash
   Authorization: Bearer <your-jwt-token>
   ```

## 🗄 Миграции базы данных

Миграции выполняются автоматически при запуске приложения с помощью Flyway. 
Они находятся в `src/main/resources/db/migration/` и выполняются в порядке номеров:

- `V1__Create_roles_and_users.sql` - создание ролей и пользователей
- `V2__Create_tokens_table.sql` - таблица для хранения токенов
- `V3__Create_other_entities_tables.sql` - остальные таблицы (фильмы, залы, сеансы и т.д.)
- `V4__Create_payments_table.sql` - таблица платежей
- `V5__Add_user_to_ticket.sql` - добавление связи пользователя с билетом
- `V6__Add_media_table.sql` - таблица для хранения медиа

## 🐛 Логирование

Логи приложения сохраняются в папку `./logs` (в Docker контейнере это `/app/logs`).
Уровни логирования настраиваются через переменные окружения:

- `LOGGING_LEVEL_COM_ABSOLUTE_CINEMA` - логирование приложения
- `LOGGING_LEVEL_SECURITY` - логирование безопасности
- `LOGGING_LEVEL_SQL` - логирование SQL запросов

## 📝 Основные контроллеры

- **AuthController** - аутентификация и авторизация
- **FilmController** - управление фильмами
- **HallController** - управление залами
- **SessionController** - управление сеансами
- **TicketController** - управление билетами
- **PurchaseController** - управление покупками
- **PaymentController** - управление платежами
- **UserController** - управление пользователями
- **ReviewController** - управление рецензиями
- **MediaController** - управление медиа файлами
- **SeatCategoryController** - управление категориями мест

## 🤝 Разработка

### Сборка проекта
```bash
mvn clean install
```

### Запуск тестов
```bash
mvn test
```

### Создание JAR файла
```bash
mvn clean package
```

## 📄 Лицензия

Проект находится под лицензией MIT.

## 👨‍💻 Автор

Deadnya